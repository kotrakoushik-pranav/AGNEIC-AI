DATABASE_URL=postgresql://postgres:password@localhost:5432/aegis_ai
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
ENVIRONMENT=development

# Set DEMO_MODE=true when running seed.py to insert simulated incidents for UI demo.
# Default is false — real mode: cameras start OFFLINE, no fake incidents.
DEMO_MODE=false
